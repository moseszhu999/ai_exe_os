{
  "name": "ai-execution-os",
  "version": "0.0.1",
  "private": true,
  "description": "Provider-safe S0 Electron and persistent browser orchestration spike",
  "main": "src/main/main.cjs",
  "scripts": {
    "check": "node scripts/check-source.cjs",
    "test": "node --test tests/*.test.cjs",
    "validate": "npm run check && npm test",
    "start": "electron .",
    "probe:ipc": "node scripts/s0-runtime-ipc-probe.cjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "electron": "43.2.0",
    "playwright": "1.62.0"
  }
}
